#version 300 es
precision mediump float;
in vec4 v_colour; 

//texture for skybox
uniform sampler2D ourTexture;

// screen effect
uniform int u_effect;

in vec2 TexCoord;

// result of shader
out vec4 result;

void main()  
{ 
	//result = vec4(TexCoord, 0,255);  // for debugging texture uv
	result = texture(ourTexture, TexCoord); 
} 
